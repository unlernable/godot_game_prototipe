# 🤝 Совместная работа над проектом

Привет! Этот файл описывает простые правила, которые помогут нам работать над проектом вместе и не ломать код друг друга.

## 🚀 Как начать (Для нового участника)

1.  **Установи Git** и **Godot 4**.
2.  **Склонируй репозиторий** к себе на компьютер:
    ```bash
    git clone https://github.com/unlernable/godot_game_prototipe.git
    ```
3.  Открой папку проекта в Godot и запусти `scenes/Main.tscn`, чтобы проверить, что всё работает.

---

## 🛠 Рабочий процесс (Workflow)

Мы используем простой цикл работы, чтобы избежать конфликтов. **Запомни порядок действий:**

### 1. СТЯНИ (PULL) ⬇️
**Перед тем как начать писать код или менять сцены**, всегда скачивай свежую версию с сервера!
```bash
git pull
```
*Если этого не сделать, ты можешь работать со старой версией, и потом твои изменения "подерутся" с изменениями друга.*

### 2. РАБОТАЙ 🔨
Вноси изменения в проект: пиши скрипты, настраивай сцены.
*Совет: Старайся не менять одни и те же файлы, которые прямо сейчас меняет другой человек, если вы не договорились.*

### 3. СОХРАНИ (COMMIT) 💾
Когда закончил логическую часть (исправил баг, добавил кнопку, настроил прыжок), сохрани изменения в историю.
```bash
git add .
git commit -m "Напиши тут понятно, что именно ты сделал"
```

### 4. ВЫЛОЖИ (PUSH) ⬆️
Отправь свои изменения на сервер, чтобы другие могли их увидеть.
```bash
git push
```

---

## 🆘 Если возникли проблемы (Конфликты)

Если при `git pull` или `git push` терминал ругается на **conflict** или **divergent branches**:
1.  Не паникуй.
2.  Свяжись с напарником.
3.  Почитайте про `git merge` или попросите помощи.

---
**Удачи в разработке!** 🎮
